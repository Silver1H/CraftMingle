<%# <div class="banner">
  <div class="container mt-5">
    <div class="title">
    <h1>Craft Mingle</h1>
    </div>
    <h4>Bring you to the skills you <strong>want</strong> to the<strong> people</strong> who have it!</h4>
    <p>Change your life and learn a new skill today.</p>
  </div>
</div> %>


<%# <div class="card"> %>

<% content_for :title, 'Courses' %>

<div class="banner d-flex justify-content-center w-100 p-3">
  <h1>Courses</h1>
</div>


  <div class="d-flex cards-container">
    <% @courses.each do |course|%>
      <%= render 'partials/coursecards', course: course %>
    <% end %>
  </div>



<div class="container mt-5">
<div type="button" class="btn btn-outline-primary">
<p> <%= link_to ' Add new course', new_course_path %> </p>
</div>

<%# </div> %>
