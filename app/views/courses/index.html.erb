<div class="banner d-flex justify-content-center mt-5">
<h1>Courses</h1>
</div>

<div class="container mt-5">
<ul>
  <% @courses.each do |course|%>
  <li><%= link_to course.subject, course_path(course) %></li>
  <% end %>
</ul>
</div>

<%# <div class="container mt-5">
<div type="button" class="btn btn-outline-primary">
<p> <%= link_to ' Add new course', new_course_path %>
<%# </p> %>
<%# </div> %>
<%# </div> %>
